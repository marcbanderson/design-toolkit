# Status

Where the work is right now. Read this first, before anything else in the repo.

Everything else records what is **true**. This records where we **are**, and it
goes stale fastest, so it lives on its own. Rewritten at the end of every working
session, and whenever position changes mid-session.

Last written: <date>

---

## Read next

| For | Read |
| --- | --- |
| The rules of this project | `<contract file>` |
| What was decided and why, what is open, what is owed | `<contract file>`, Decisions |
| How Marc decides | `~/.claude/design-judgment.md` |

---

## Position

Branch, PR state, what is pushed and what is not, what is deployed. Verify these
with commands rather than memory, and never write a fact a command can confirm
unless it carries meaning on its own.

## In flight

Anything half-done, with enough detail to resume. "Nothing half-edited, tree is
clean" is a valid and useful entry.

## Next

The actual next actions, in order. Not aspirations. Anything blocked on Marc
goes first, because it is the only part he can clear.

## Traps

Things that have already cost a round trip. Check these before believing a
result. This section only grows when something bites, which is what makes it the
most valuable part of the file.

## Known broken, not ours to fix yet

Pre-existing defects, so they are never rediscovered as news.
